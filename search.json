[
  {
    "objectID": "pages/example.html",
    "href": "pages/example.html",
    "title": "Examples",
    "section": "",
    "text": "import os\nimport snippet_cast\n\n\n%%snippet-cast --help\n\n  %%snippet-cast [-o PATH] [-n NAME] [-d DIR]\n                    [--tts {say,silent,piper,elevenlabs,manual}] [--no-trace]\n                    [--every | --no-every] [--subtitles | --no-subtitles]\n                    [--typing | --no-typing] [--typing-speed SECONDS]\n                    [--style NAME] [--bg-color HEX] [--highlight-color HEX]\n                    [--screenflow [WxH]] [--font-size PX]\n                    [--state-bg-color HEX] [--state-fg-color HEX]\n                    [--pause SECONDS] [--manual-audio-dir DIR]\n                    [--export-script | --no-export-script]\n                    [--record | --no-record] [--responsive | --no-responsive]\n                    [--light-controls | --no-light-controls] [-h]\n                    [--order {source,exec}] [-q | --quiet | --no-quiet]\n                    [--no-frame] [--embed]\n\nRender `cell` (an annotated Python snippet) into a screencast and\ndisplay it inline. See the module docstring for a full example.\n\nRegistered as a LINE magic too, purely so `--help` can be asked for on\nits own. IPython refuses a `%%`-form cell whose body is empty — the\ncheck is `cell == ''` in `InteractiveShell.run_cell_magic`, which\nraises before any magic function is reached, so `%%snippet-cast\n--help` with nothing under it cannot be handled here. Registering the\nname as a line magic is IPython's own remedy: `%snippet-cast --help`\nworks, and its refusal message then ends \"Did you mean the line magic\n%snippet-cast (single %)?\" instead of leaving the reader stuck.\n\noptions:\n  -o PATH, --output PATH\n                        explicit output MP4 path — overrides -n/--name and\n                        -d/--output-dir if given\n  -n NAME, --name NAME  basename (without extension) for the output file in\n                        --output-dir, when -o/--output isn't given [default:\n                        out; env: SNIPPET_CAST_NAME]\n  -d DIR, --output-dir DIR\n                        directory for the output file when -o/--output isn't\n                        given (created if missing) [default: current\n                        directory; env: SNIPPET_CAST_OUTPUT_DIR]\n  --tts &lt;{say,silent,piper,elevenlabs,manual}&gt;\n                        TTS backend [default: silent here — always works, no\n                        setup; see SETUP.md for piper/elevenlabs; env:\n                        SNIPPET_CAST_TTS] (--record implies manual; passing\n                        --tts explicitly as anything else together with\n                        --record is an error)\n  --no-trace            don't execute the snippet; skip the state panel [env:\n                        SNIPPET_CAST_NO_TRACE]\n  --every, --no-every   one beat per execution of a line (animates loops)\n                        [env: SNIPPET_CAST_EVERY]\n  --subtitles, --no-subtitles\n                        burn the narration text as a caption [env:\n                        SNIPPET_CAST_SUBTITLES]\n  --typing, --no-typing\n                        type newly revealed lines character-by-character [env:\n                        SNIPPET_CAST_TYPING]\n  --typing-speed SECONDS\n                        seconds to reveal each newly typed character [default:\n                        0.1; env: SNIPPET_CAST_TYPING_SPEED]\n  --style NAME          syntax highlighting theme: a built-in name (dark-\n                        modern, light-modern, numpy), any pygments style name,\n                        or a path to a Pandoc/KDE .theme file [default: numpy;\n                        env: SNIPPET_CAST_STYLE]\n  --bg-color HEX        background behind the code and canvas as '#rrggbb',\n                        overriding the style's own; 'none' uses the style's\n                        [default: None; env: SNIPPET_CAST_BG_COLOR]\n  --highlight-color HEX\n                        band behind the highlighted code line as '#rrggbb',\n                        overriding the style's own; 'panel' matches the STATE\n                        panel background (so the two read as one surface),\n                        'none' uses the style's [default: panel; env:\n                        SNIPPET_CAST_HIGHLIGHT_COLOR]\n  --screenflow &lt;[WxH]&gt;  render onto a fixed frame of this size with the\n                        content centred, instead of a canvas sized to the\n                        snippet — for dropping straight onto a video-editor\n                        timeline. Nothing is scaled, so text stays crisp [bare\n                        flag: 1920x1080; env: SNIPPET_CAST_SCREENFLOW]\n  --font-size PX        code font size in pixels; the state panel and captions\n                        scale with it, each keeping its own offset [default:\n                        26; env: SNIPPET_CAST_FONT_SIZE]\n  --state-bg-color HEX  background of the state panel as '#rrggbb'; 'none'\n                        keeps the default [default: #DDDEDF; env:\n                        SNIPPET_CAST_STATE_BG_COLOR]\n  --state-fg-color HEX  text in the state panel as '#rrggbb' — names and\n                        values both; 'none' keeps the default scheme [env:\n                        SNIPPET_CAST_STATE_FG_COLOR]\n  --pause SECONDS       seconds of silence held on each beat's frame after its\n                        narration (in two-pass mode, also between the two\n                        passes); giving this explicitly also allows a cell\n                        with NO '#:' narration at all, rendering one silent\n                        frame per code line held for this long, to narrate\n                        later [default: 0.8; env: SNIPPET_CAST_PAUSE]\n  --manual-audio-dir DIR\n                        directory of pre-recorded audio for --tts manual\n                        [default: ./manual_audio; env:\n                        SNIPPET_CAST_MANUAL_AUDIO_DIR]\n  --export-script, --no-export-script\n                        print the ordered narration script instead of\n                        rendering [env: SNIPPET_CAST_EXPORT_SCRIPT]\n  --record, --no-record\n                        interactively record narration via the system\n                        microphone (macOS only), then build with --tts manual\n                        (implied automatically); see SETUP.md [env:\n                        SNIPPET_CAST_RECORD]\n  --responsive, --no-responsive\n                        style the displayed &lt;video&gt; so it never exceeds the\n                        width of whatever it is rendered into (Quarto HTML, a\n                        wide notebook, a narrow column) instead of keeping the\n                        exact pixel size the snippet happened to produce; on\n                        by default, --no-responsive keeps the intrinsic size\n                        [default: True; env: SNIPPET_CAST_RESPONSIVE]\n  --light-controls, --no-light-controls\n                        keep the video controls' glyphs light (white) instead\n                        of flipping them dark — for a dark theme such as\n                        --style monokai. Left unset the right one is chosen\n                        from the frame's own background, so this is rarely\n                        needed [env: SNIPPET_CAST_LIGHT_CONTROLS]\n  -h, --help            show this message, with every option and its default,\n                        and stop without rendering the cell\n  --order &lt;{source,exec}&gt;\n                        playback order of the narrated lines: 'source' (top to\n                        bottom, or the 'N) ' order the cell gives) or 'exec'\n                        (the order Python visits them — each line highlighted\n                        on entry with its pre-state, then again on completion,\n                        where the narration plays) [default: source; env:\n                        SNIPPET_CAST_ORDER]\n  -q, --quiet, --no-quiet\n                        suppress progress, notes and the cell's own output;\n                        errors still print, and --export-script still returns\n                        its script [env: SNIPPET_CAST_QUIET]\n  --no-frame            with --record, don't pop each beat's rendered frame in\n                        the system image viewer [env: SNIPPET_CAST_NO_FRAME]\n  --embed               embed the video as base64 in the notebook instead of\n                        linking the file\n\n\n\n%%snippet-cast --highlight-color #DDDEDF --state-bg-color #DDDEDF \ndef add_one(n):                                                                         #: 3) The first line \"names\" the function and define the parameter \"n\".            / 2) The function call passes the value 7 to the function parameter \"n\".\n    x = n + 1                                                                           #: 4) The second line adds one.                       / 3) Define \"x\" as \"n\" plus one. \"x\" is a temporary function variable that lives only while the function runs.\n    return x                                                                            #: 5) The last line returns the value of x.           / 4) The function returns the value pointed to by x.\n\nassert add_one(7) == 8                                                                  #: 2) Begin by writing a test that calls the function / 1) We call the function.\n                                                                                        #: 1) Let's write a function that adds one.            / 5) The temporary function context disappears, leaving only the returned value where the function was called.\n\nnote: 5 line(s) have an entry narration (text before a single '/') that only --order exec shows: 6, 5, 1, 2, 3. If you meant a two-pass narration, the separator is '//'.\n5 beats -&gt; .snippet-cast/0ec520e137ed.mp4  (backend: silent, trace: on, first-exec)\n  [1/5] 5) The temporary function context disappears, leaving only t\n  [2/5] 1) We call the function.\n  [3/5] 2) The function call passes the value 7 to the function para\n  [4/5] 3) Define \"x\" as \"n\" plus one. \"x\" is a temporary function v\n  [5/5] 4) The function returns the value pointed to by x.\ndone.\n\n\n\n      Your browser does not support the video element.\n    \n\n\nUsing –order exec, It would be useful if the single-pass mode and second pass in two-pass mode could have separate narrations for entry visits and completion visits. Maybe replace “/” separating passes in two-pass mode with “//” so “/” could be used to separate entry and completion narrations:\nAllowed formats with single-pass mode:\nnarration entry narration / completion narration\nAllowed formats with two-pass mode:\nfirst-pass narration // second-pass narration first-pass narration // second-pass entry narration / second-pass completion narration\n\n%%snippet-cast\n                 #: 0) blah blah\ndef add_one(n):  #: 2) blah blah\n    x = n + 1    #: 3) blah blah\n    return x     #: 4) blah blah\n\nx = 2            #: 1) blah blah\ny = add_one(x)   #: 5) blah blah\ny = y + x        #: 6) blah blah\nprint(y)         #: 7) blah blah\n\n5\n8 beats -&gt; .snippet-cast/b9f40c3d59c8.mp4  (backend: silent, trace: on, first-exec)\n  [1/8] blah blah\n  [2/8] blah blah\n  [3/8] blah blah\n  [4/8] blah blah\n  [5/8] blah blah\n  [6/8] blah blah\n  [7/8] blah blah\n  [8/8] blah blah\ndone.\n\n\n\n      Your browser does not support the video element.\n    \n\n\n\n%%snippet-cast --order exec\n                  #: blah blah\ndef add_one(n):   #: blah blah\n    if n &lt; 4:     #: blah blah\n        x = n + 1 #: blah blah\n    else:         #: blah blah\n        x = n     #: blah blah\n    return x      #: blah blah\n\nx = 2             #: blah blah\ny = add_one(x)    #: blah blah\nprint(y)          #: blah blah\n\n3\nnote: --order exec has no beat for 2 narrated line(s) that never ran to completion (5, 6) — an untaken branch, or the snippet stopped early. Their code is still shown; only the narration is dropped.\n12 beats -&gt; .snippet-cast/147c3525ecd2.mp4  (backend: silent, trace: on, first-exec)\n  [1/12] blah blah\n  [2/12] blah blah\n  [3/12] x = 2\n  [4/12] blah blah\n  [5/12] y = add_one(x)\n  [6/12] def add_one(n):\n  [7/12] blah blah\n  [8/12] x = n + 1\n  [9/12] blah blah\n  [10/12] blah blah\n  [11/12] blah blah\n  [12/12] blah blah\ndone.\n\n\n\n      Your browser does not support the video element.\n    \n\n\n\n%%snippet-cast --tts say --order exec --highlight-color #DDDEDF --state-bg-color #DDDEDF \n                  #: typing // completing\ndef add_one(n):   #: typing // function call / function definition \n    if n &lt; 4:     #: typing // expression / assignment \n        x = n + 1 #: typing // expression / assignment \n    else:\n        x = n     #: typing // expression / assignment  \n    return x      #: typing // expression / return \n\nx = 2             #: typing // expression / assignment \ny = add_one(x)    #: typing // function call / completing \nprint(y)          #: typing // expression / print \n\n3\nnote: --order exec has no beat for 1 narrated line(s) that never ran to completion (6) — an untaken branch, or the snippet stopped early. Their code is still shown; only the narration is dropped.\n24 beats (9 pass-1 + 15 pass-2) -&gt; .snippet-cast/c6cdaf0b0630.mp4  (backend: say, trace: on, two-pass)\n  [pass1 1/9] typing\n  [pass1 2/9] typing\n  [pass1 3/9] typing\n  [pass1 4/9] typing\n  [pass1 5/9] typing\n  [pass1 6/9] typing\n  [pass1 7/9] typing\n  [pass1 8/9] typing\n  [pass1 9/9] typing\n  [pass2 1/15] completing\n  [pass2 2/15] function definition\n  [pass2 3/15] expression\n  [pass2 4/15] assignment\n  [pass2 5/15] function call\n  [pass2 6/15] function call\n  [pass2 7/15] expression\n  [pass2 8/15] assignment\n  [pass2 9/15] expression\n  [pass2 10/15] assignment\n  [pass2 11/15] expression\n  [pass2 12/15] return\n  [pass2 13/15] completing\n  [pass2 14/15] expression\n  [pass2 15/15] print\ndone.\n\n\n\n      Your browser does not support the video element.\n    \n\n\n\n%%snippet-cast --every\ndef add_one(n): #: blah blah\n    x = n + 1      #: blah blah\n    return x  #: blah blah\n\nfor x in range(3):    #: blah blah\n    print(add_one(x))  #: blah blah   \n\n1\n2\n3\n13 beats -&gt; .snippet-cast/1d74c688cb5a.mp4  (backend: silent, trace: on, every-exec)\n  [1/13] blah blah\n  [2/13] blah blah\n  [3/13] blah blah\n  [4/13] blah blah\n  [5/13] blah blah\n  [6/13] blah blah\n  [7/13] blah blah\n  [8/13] blah blah\n  [9/13] blah blah\n  [10/13] blah blah\n  [11/13] blah blah\n  [12/13] blah blah\n  [13/13] blah blah\ndone.\n\n\n\n      Your browser does not support the video element.\n    \n\n\n\n%%snippet-cast -q\n\nprint(\"Hello, world!\") #: Hello world\n\n\n      Your browser does not support the video element.\n    \n\n\n\n! open videos\n\n\n%%snippet-cast --help\nx\n\n\n%%snippet-cast -n hello --tts say\nprint(\"hello world\") #: Let's make our first program. /\n\n\n%%snippet-cast -n hello --typing-speed 0.1 --tts say\nprint(\"hello world\") #: / \"print\" prints the string\n\n\n%%snippet-cast -n hello --typing-speed 2 --tts say\nprint(\"hello world\") #: Let's make our first program. / \"print\" prints the string\n\n\n%%snippet-cast --pause 2 --every --screenflow --font-size 40 \n\nx, z, y = 0, 0, 0\nfor i in range(3):\n    x += i\n\n\n%%snippet-cast --tts say --pause 2 --typing-speed 2\n#| fig-column: margin\nprint(\"hello world\") #: let's make our first program / \"print\" prints the string\nprint(\"hi kasper\") #: let's do another one / \"print\" prints the other string\n\n\n%%snippet-cast --tts say --pause 0.6 --typing-speed 0.1 -n fibster\n                        #: 0) \ndef fib(n):             #: 2) \n    a, b = 0, 1         #: 3) / Start from the first two Fibonacci numbers, zero and one.\n    for _ in range(n):  #: 4) / Loop \"n\" times; the counter itself is unused.\n        a, b = b, a + b #: 5) / Advance the pair — \"b\" becomes the running sum.\n    return a            #: 6) / After the loop, a holds the nth Fibonacci number.\n\nassert fib(7) == 13    #: 1) \n\n#: 0) Let's write a Fibonacci function / Now, let us trace the function as it runs.\n\n#: 1) Always begin by writing a test of the function you want to create. That\n# has the added benefit of running your function so any print\n# statements you add as you develop it are executed, ensuring your\n# implementation works as expected. / Call the function with seven as\n# argument and that that it returns 13 - as it should. becomes\n# thirteen.\n\n#: 2) The first line \"names\" the \"fib\" function and defines its\n# parameters - in this case a single one named n. The \"n\" variable\n# can be used in the function body to refer to the value argument\n# passed to the function. The line ends with a colon to show that it\n# controls the lines indented statements below it. These are the\n# statements that are run when the function is called. / When the\n# function is called, the function parameter \"n\" captures the value\n# of the argument passed to the function - in this case \"seven\".\n\n#: 3) / Start from the first two Fibonacci numbers, zero and one.\n\n\n%%snippet-cast --tts say --pause 0.6 --typing-speed 0.1 -n fibster\n                        #: 0) \ndef fib(n):             #: 2) \n    a, b = 0, 1         #: 3) / Start from the first two Fibonacci numbers, zero and one.\n    for _ in range(n):  #: 4) / Loop \"n\" times; the counter itself is unused.\n        a, b = b, a + b #: 5) / Advance the pair — \"b\" becomes the running sum.\n    return a            #: 6) / After the loop, a holds the nth Fibonacci number.\n\nassert fib(7) == 13    #: 1) \n\n\n%%snippet-cast --tts manual --record -n forl\n#%%snippet-cast --tts say -n forl\n#: 1) Lets compute the Fibonacci numbers / Let's trace an iterative Fibonacci function as it runs.\ndef fib(n):             #: 3) Now, define the function. / The \"first\" line names the \"fib\" function and defines its parameters.\n    a, b = 0, 1         #: 4) \"a\" and \"b\" are our first two Fibonacci numbers / Start from the first two Fibonacci numbers, zero and one.\n    for _ in range(n):  #: 5) / Loop \"n\" times;`` the counter itself is unused.\n        a, b = b, a + b #: 6) / Advance the pair — \"b\" becomes the running sum.\n    return a            #: 7) / After the loop, \"a\" holds the nth Fibonacci number.\n\nresult = fib(7)         #: 2) We call our function \"fib\" and make it take a single argument / Call fib with seven; result becomes thirteen.\n\n\nfrom IPython.display import Video\nVideo('out.mp4')\n\n\n%%snippet-cast --tts piper --pause 0.6 --typing-speed 0.1 -o out.mp4 \n\n#: 0) Let's write a Fibonacci function / Let's trace an function as it runs.\ndef fib(n):             #: 2) / The first line names the fib-function and defines its parameters - in this case a single one named n. The first line ends with a colon and the code that runs when the function is called is what is indented on the lines below. \n    a, b = 0, 1         #: 3) / ... Start from the first two Fibonacci numbers, zero and one.\n    for _ in range(n):  #: 4) / ... Loop n times; the counter itself is unused.\n        a, b = b, a + b #: 5) / ... Advance the pair — b becomes the running sum.\n    return a            #: 6) / ... After the loop, a holds the nth Fibonacci number.\n\nresult = fib(7)         #: 1) / ... Call fib with seven; result becomes thirteen.\n\n\n\nsnippet-cast tmp.py -o out.mp4 --typing --tts piper --piper-model ~/piper-voices/en_US-lessac-medium.onnx --piper-length-scale 1.5 --piper-data-dir ~/piper-voices --pause 0.6 --typing-speed 0.1 --every",
    "crumbs": [
      "Examples",
      "Examples"
    ]
  },
  {
    "objectID": "api/build.html",
    "href": "api/build.html",
    "title": "build",
    "section": "",
    "text": "snippet_cast.screencast.build(\n    source_path,\n    out_path,\n    tts,\n    trace=True,\n    every=False,\n    subtitles=False,\n    typing=False,\n    typing_speed=TYPE_SPEED,\n    pause=PAUSE_DEFAULT,\n    manual_audio_dir=None,\n    style=None,\n    bg_color=_USE_DEFAULT,\n    state_bg_color=None,\n    state_fg_color=None,\n    highlight_color=_USE_DEFAULT,\n    allow_unnarrated=False,\n    font_size=None,\n    screenflow=None,\n    quiet=False,\n)\nRender an annotated Python snippet into a narrated screencast video.\nParses source_path for trailing #: narration comments, optionally executes it to capture a per-line variable state, and assembles one still-frame-plus-narration clip per beat into out_path with ffmpeg.\n\n\n\nsource_path : \n\nPath to the annotated, still-valid Python snippet to narrate.\n\nout_path : \n\nPath the assembled MP4 is written to.\n\ntts : \n\nName of a registered TTS backend (a key of BACKENDS): one of \"say\", \"silent\", \"piper\", \"elevenlabs\", \"manual\".\n\ntrace :  = True\n\nExecute the snippet under sys.settrace to capture the state panel. Required for every=True.\n\nevery :  = False\n\nEmit one beat per execution of a marked line (animates loops iteration by iteration) instead of one beat per marked line. Not supported together with two-pass narration (a / in some marker).\n\nsubtitles :  = False\n\nBurn the narration text onto each frame as a caption.\n\ntyping :  = False\n\nType newly revealed lines character-by-character (first-execution mode only; has no effect when every=True, and no effect in two-pass mode — the writing pass always types).\n\ntyping_speed :  = TYPE_SPEED\n\nSeconds to reveal each newly typed character when typing=True, or when the writing pass has no narration to time itself to [default: TYPE_SPEED]. Larger is slower.\n\npause :  = PAUSE_DEFAULT\n\nSeconds of silence to hold on each beat’s frame after its narration finishes, before the next beat begins [default: PAUSE_DEFAULT]. 0 cuts directly from one beat’s narration to the next. In two-pass mode it applies within both passes and at the seam between them (the writing pass holds its finished code for pause seconds before the walkthrough starts). Never appended after the video’s final beat. With allow_unnarrated=True on a snippet that has no narration at all, it is instead the full length of every beat.\n\nallow_unnarrated :  = False\n\nRender a snippet with no #: comments at all instead of exiting with “No narration found”: one beat per code line, progressively revealed, each held for pause seconds with no audio synthesized (no TTS backend is used, whatever tts says). Intended for producing a silent, evenly paced screencast to narrate afterwards in a video editor. Requires pause &gt; 0, since it is the whole length of each beat. The CLI turns this on exactly when --pause is given explicitly (or via SNIPPET_CAST_PAUSE), so a forgotten #: still reports the error rather than silently rendering.\n\nfont_size :  = None\n\nCode font size in pixels for this render [default: FONT_SIZE]. The state panel and the captions scale with it, each keeping the offset it has from FONT_SIZE in the module constants — so a PANEL_FONT_SIZE edited to differ stays that much apart. None means “use FONT_SIZE”, which renders exactly as it did before this option existed. Values below FONT_SIZE_MIN are clamped (the CLI rejects them outright). Changing it resizes the whole canvas, which is fine — plan_canvas() measures the frame at this size — but it must be decided once per video, never per frame (invariant 1).\n\nscreenflow :  = None\n\nTarget frame as a (width, height) pair (see resolve_screenflow_arg()), or None for a canvas sized to the snippet. The content block keeps its natural size and is CENTRED in that frame — nothing is scaled, so text stays crisp and the caption band stays under the code rather than pinned to the frame’s bottom edge. A snippet whose natural canvas is larger than the frame raises ValueError (the CLI turns that into an exit naming a font_size that would fit) rather than being silently shrunk.\n\nquiet :  = False\n\nSuppress every progress line, note and trace warning — and whatever the snippet itself prints while being traced. Errors are NOT suppressed: they still raise/sys.exit to stderr, so a quiet run that fails can’t be mistaken for one that succeeded.\n\nmanual_audio_dir :  = None\n\nDirectory of pre-recorded audio files for tts=\"manual\", named 001.wav, 002.wav, … (or .mp3/.m4a/.aiff/.flac/.ogg) matching export_script()’s numbering.\n\nstyle :  = None\n\nSyntax highlighting theme for this render: a registered pygments style name, a BUILTIN_STYLES key (\"dark-modern\"/\"light-modern\"), a path to a KSyntaxHighlighting/Pandoc .theme file (see load_theme()), or a pygments.style.Style subclass. None uses the STYLE global.\n\nhighlight_color :  = _USE_DEFAULT\n\n\"#rrggbb\" band behind the highlighted code line, overriding the style’s own highlight_color. HIGHLIGHT_PANEL (\"panel\", the shipped HIGHLIGHT_COLOR default) tracks the STATE panel background, including a per-run state_bg_color, so the band and the box read as one surface. None uses the style’s; omit the argument to use the HIGHLIGHT_COLOR global. Worth setting for a style that declares none — pygments’ unset default is a pale #ffffcc (DarkModernStyle/LightModernStyle are both in that boat); a style loaded from a .theme file gets one derived instead.\n\nstate_bg_color :  = None\n\n\"#rrggbb\" background for the state panel, overriding PANEL_BG. None keeps it.\n\nstate_fg_color :  = None\n\n\"#rrggbb\" for ALL of the state panel’s text — variable names and values both — with the “STATE” header blended HEADER_DIM toward the panel background so it stays subordinate. None keeps the default three-color scheme (COL_NAME/COL_VALUE/COL_HEADER), which is also where to go for finer control than one color gives.\n\nbg_color :  = _USE_DEFAULT\n\n\"#rrggbb\" background behind both the code and the canvas, overriding whatever background style declares (its syntax colors are left alone — pairing a dark background with a light style is on you). None means no override, i.e. the style’s own background; omit the argument entirely to use the BG_COLOR global. Caption and rule colors adapt to whichever background wins, via _is_light().\n\n\n\n\n\nA #: narration containing a / is split into “writing pass / walkthrough pass” text (see split_narration). If ANY marker in the file uses /, the whole video becomes two full, sequential passes: first the entire snippet is typed in (narrated by each line’s part before /, no state panel, no highlight), then the existing walkthrough plays again from the top (narrated by the text after /, exactly today’s single-pass mechanics). A file with no / anywhere renders exactly as before.\n\n\n\nA run of 2+ consecutive periods inside a #: narration (“..” , “….”, …) inserts a pause mid-line instead of being spoken: PAUSE_PER_PERIOD seconds of silence per period (“..” -&gt; 0.2s, “….” -&gt; 0.4s), with the text on either side synthesized separately and stitched together (see _synth_with_pauses). A single period is ordinary end-of-sentence punctuation and is untouched. tts=\"say\" handles this differently — it rewrites the run in place to say’s own [[slnc N]] pause markup and synthesizes the whole line in one call instead (better prosody than two stitched fragments), and separately flanks any run of 2+ ALL-CAPS words with say’s [[emph +]] ... [[emph -]] (e.g. “Please NEVER DO THAT AGAIN” -&gt; “Please [[emph +]] NEVER DO THAT AGAIN [[emph -]]”) — see _say_markup. The manual backend ignores both: a human recording narration reads the dots as a pause cue and the caps as emphasis directly, and rewriting/splitting would desync --tts manual’s file numbering with export_script()’s.\n\n\n\nfrom snippet_cast import build\n\nbuild(\"fib.py\", \"out.mp4\", tts=\"silent\", subtitles=True)\n\n\n\nsnippet_cast.screencast.main snippet_cast.screencast.export_script",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/build.html#parameters",
    "href": "api/build.html#parameters",
    "title": "build",
    "section": "",
    "text": "source_path : \n\nPath to the annotated, still-valid Python snippet to narrate.\n\nout_path : \n\nPath the assembled MP4 is written to.\n\ntts : \n\nName of a registered TTS backend (a key of BACKENDS): one of \"say\", \"silent\", \"piper\", \"elevenlabs\", \"manual\".\n\ntrace :  = True\n\nExecute the snippet under sys.settrace to capture the state panel. Required for every=True.\n\nevery :  = False\n\nEmit one beat per execution of a marked line (animates loops iteration by iteration) instead of one beat per marked line. Not supported together with two-pass narration (a / in some marker).\n\nsubtitles :  = False\n\nBurn the narration text onto each frame as a caption.\n\ntyping :  = False\n\nType newly revealed lines character-by-character (first-execution mode only; has no effect when every=True, and no effect in two-pass mode — the writing pass always types).\n\ntyping_speed :  = TYPE_SPEED\n\nSeconds to reveal each newly typed character when typing=True, or when the writing pass has no narration to time itself to [default: TYPE_SPEED]. Larger is slower.\n\npause :  = PAUSE_DEFAULT\n\nSeconds of silence to hold on each beat’s frame after its narration finishes, before the next beat begins [default: PAUSE_DEFAULT]. 0 cuts directly from one beat’s narration to the next. In two-pass mode it applies within both passes and at the seam between them (the writing pass holds its finished code for pause seconds before the walkthrough starts). Never appended after the video’s final beat. With allow_unnarrated=True on a snippet that has no narration at all, it is instead the full length of every beat.\n\nallow_unnarrated :  = False\n\nRender a snippet with no #: comments at all instead of exiting with “No narration found”: one beat per code line, progressively revealed, each held for pause seconds with no audio synthesized (no TTS backend is used, whatever tts says). Intended for producing a silent, evenly paced screencast to narrate afterwards in a video editor. Requires pause &gt; 0, since it is the whole length of each beat. The CLI turns this on exactly when --pause is given explicitly (or via SNIPPET_CAST_PAUSE), so a forgotten #: still reports the error rather than silently rendering.\n\nfont_size :  = None\n\nCode font size in pixels for this render [default: FONT_SIZE]. The state panel and the captions scale with it, each keeping the offset it has from FONT_SIZE in the module constants — so a PANEL_FONT_SIZE edited to differ stays that much apart. None means “use FONT_SIZE”, which renders exactly as it did before this option existed. Values below FONT_SIZE_MIN are clamped (the CLI rejects them outright). Changing it resizes the whole canvas, which is fine — plan_canvas() measures the frame at this size — but it must be decided once per video, never per frame (invariant 1).\n\nscreenflow :  = None\n\nTarget frame as a (width, height) pair (see resolve_screenflow_arg()), or None for a canvas sized to the snippet. The content block keeps its natural size and is CENTRED in that frame — nothing is scaled, so text stays crisp and the caption band stays under the code rather than pinned to the frame’s bottom edge. A snippet whose natural canvas is larger than the frame raises ValueError (the CLI turns that into an exit naming a font_size that would fit) rather than being silently shrunk.\n\nquiet :  = False\n\nSuppress every progress line, note and trace warning — and whatever the snippet itself prints while being traced. Errors are NOT suppressed: they still raise/sys.exit to stderr, so a quiet run that fails can’t be mistaken for one that succeeded.\n\nmanual_audio_dir :  = None\n\nDirectory of pre-recorded audio files for tts=\"manual\", named 001.wav, 002.wav, … (or .mp3/.m4a/.aiff/.flac/.ogg) matching export_script()’s numbering.\n\nstyle :  = None\n\nSyntax highlighting theme for this render: a registered pygments style name, a BUILTIN_STYLES key (\"dark-modern\"/\"light-modern\"), a path to a KSyntaxHighlighting/Pandoc .theme file (see load_theme()), or a pygments.style.Style subclass. None uses the STYLE global.\n\nhighlight_color :  = _USE_DEFAULT\n\n\"#rrggbb\" band behind the highlighted code line, overriding the style’s own highlight_color. HIGHLIGHT_PANEL (\"panel\", the shipped HIGHLIGHT_COLOR default) tracks the STATE panel background, including a per-run state_bg_color, so the band and the box read as one surface. None uses the style’s; omit the argument to use the HIGHLIGHT_COLOR global. Worth setting for a style that declares none — pygments’ unset default is a pale #ffffcc (DarkModernStyle/LightModernStyle are both in that boat); a style loaded from a .theme file gets one derived instead.\n\nstate_bg_color :  = None\n\n\"#rrggbb\" background for the state panel, overriding PANEL_BG. None keeps it.\n\nstate_fg_color :  = None\n\n\"#rrggbb\" for ALL of the state panel’s text — variable names and values both — with the “STATE” header blended HEADER_DIM toward the panel background so it stays subordinate. None keeps the default three-color scheme (COL_NAME/COL_VALUE/COL_HEADER), which is also where to go for finer control than one color gives.\n\nbg_color :  = _USE_DEFAULT\n\n\"#rrggbb\" background behind both the code and the canvas, overriding whatever background style declares (its syntax colors are left alone — pairing a dark background with a light style is on you). None means no override, i.e. the style’s own background; omit the argument entirely to use the BG_COLOR global. Caption and rule colors adapt to whichever background wins, via _is_light().",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/build.html#narration-split-into-two-passes",
    "href": "api/build.html#narration-split-into-two-passes",
    "title": "build",
    "section": "",
    "text": "A #: narration containing a / is split into “writing pass / walkthrough pass” text (see split_narration). If ANY marker in the file uses /, the whole video becomes two full, sequential passes: first the entire snippet is typed in (narrated by each line’s part before /, no state panel, no highlight), then the existing walkthrough plays again from the top (narrated by the text after /, exactly today’s single-pass mechanics). A file with no / anywhere renders exactly as before.",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/build.html#inline-pauses-and-for-say-emphasis-within-a-narration-line",
    "href": "api/build.html#inline-pauses-and-for-say-emphasis-within-a-narration-line",
    "title": "build",
    "section": "",
    "text": "A run of 2+ consecutive periods inside a #: narration (“..” , “….”, …) inserts a pause mid-line instead of being spoken: PAUSE_PER_PERIOD seconds of silence per period (“..” -&gt; 0.2s, “….” -&gt; 0.4s), with the text on either side synthesized separately and stitched together (see _synth_with_pauses). A single period is ordinary end-of-sentence punctuation and is untouched. tts=\"say\" handles this differently — it rewrites the run in place to say’s own [[slnc N]] pause markup and synthesizes the whole line in one call instead (better prosody than two stitched fragments), and separately flanks any run of 2+ ALL-CAPS words with say’s [[emph +]] ... [[emph -]] (e.g. “Please NEVER DO THAT AGAIN” -&gt; “Please [[emph +]] NEVER DO THAT AGAIN [[emph -]]”) — see _say_markup. The manual backend ignores both: a human recording narration reads the dots as a pause cue and the caps as emphasis directly, and rewriting/splitting would desync --tts manual’s file numbering with export_script()’s.",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/build.html#examples",
    "href": "api/build.html#examples",
    "title": "build",
    "section": "",
    "text": "from snippet_cast import build\n\nbuild(\"fib.py\", \"out.mp4\", tts=\"silent\", subtitles=True)",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/build.html#see-also",
    "href": "api/build.html#see-also",
    "title": "build",
    "section": "",
    "text": "snippet_cast.screencast.main snippet_cast.screencast.export_script",
    "crumbs": [
      "Screencast API",
      "build"
    ]
  },
  {
    "objectID": "api/index.html",
    "href": "api/index.html",
    "title": "Function reference",
    "section": "",
    "text": "Programmatic interface for turning an annotated Python snippet into a narrated screencast video.\n\n\n\nbuild\nRender an annotated Python snippet into a narrated screencast video.",
    "crumbs": [
      "Function reference"
    ]
  },
  {
    "objectID": "api/index.html#screencast-api",
    "href": "api/index.html#screencast-api",
    "title": "Function reference",
    "section": "",
    "text": "Programmatic interface for turning an annotated Python snippet into a narrated screencast video.\n\n\n\nbuild\nRender an annotated Python snippet into a narrated screencast video.",
    "crumbs": [
      "Function reference"
    ]
  },
  {
    "objectID": "pages/overview.html",
    "href": "pages/overview.html",
    "title": "Overview",
    "section": "",
    "text": "snippet-cast turns an annotated Python snippet into a narrated screencast video. Narration is written as trailing #: comments on the snippet’s own lines, so the input stays valid, runnable Python. The tool renders syntax-highlighted code frames with a progressive reveal, a live variable state panel, optional burned-in captions and a typing-in animation, synthesises speech per line, and stitches everything into an MP4 with ffmpeg.",
    "crumbs": [
      "Overview"
    ]
  },
  {
    "objectID": "pages/overview.html#installation",
    "href": "pages/overview.html#installation",
    "title": "Overview",
    "section": "Installation",
    "text": "Installation\n\npixicondapip\n\n\npixi workspace channel add munch-group\npixi add snippet-cast\n\n\nconda install -c munch-group snippet-cast\n\n\npip install snippet-cast",
    "crumbs": [
      "Overview"
    ]
  },
  {
    "objectID": "pages/overview.html#basic-use",
    "href": "pages/overview.html#basic-use",
    "title": "Overview",
    "section": "Basic use",
    "text": "Basic use\nsnippet-cast snippet.py -o out.mp4 --tts silent --subtitles",
    "crumbs": [
      "Overview"
    ]
  },
  {
    "objectID": "pages/overview.html#demo",
    "href": "pages/overview.html#demo",
    "title": "Overview",
    "section": "Demo",
    "text": "Demo\n--every animates a loop iteration by iteration instead of revealing the snippet line by line, rendered here with --tts silent --subtitles:\nsnippet-cast loop.py -o loop_every.mp4 --every --subtitles --tts silent",
    "crumbs": [
      "Overview"
    ]
  },
  {
    "objectID": "pages/overview.html#some-use-case",
    "href": "pages/overview.html#some-use-case",
    "title": "Overview",
    "section": "Some use case",
    "text": "Some use case\nThe build function is the programmatic entry point behind the snippet-cast CLI — call it directly from Python to render a video without shelling out.\n\nfrom snippet_cast import build\n\n# build(\"snippet.py\", \"out.mp4\", tts=\"silent\", subtitles=True)",
    "crumbs": [
      "Overview"
    ]
  },
  {
    "objectID": "pages/overview.html#learning-more",
    "href": "pages/overview.html#learning-more",
    "title": "Overview",
    "section": "Learning more",
    "text": "Learning more\nSee SETUP.md for configuring the Piper and ElevenLabs text-to-speech backends.",
    "crumbs": [
      "Overview"
    ]
  }
]